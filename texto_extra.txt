    <div className="container-doc">
      <h2 className="container-form">📁 Documentos Facturas</h2>

      {/* Subir archivos */}
      <input
        type="file"
        multiple
        onChange={handleUpload}
        className="mb-4"
      />

      {/* Lista de documentos */}
      <ul className="container">
        {documentos.map((doc, index) => (
          <li key={index} className="flex justify-between items-center border p-2 rounded shadow-sm">
            <span>{doc.nombre}</span>
            <a
              href={doc.url}
              download={doc.nombre}
              className="bg-blue-600 text-white px-3 py-1 rounded hover:bg-blue-700 text-sm"
            >
              Descargar
            </a>
          </li>
        ))}
      </ul>
    </div>